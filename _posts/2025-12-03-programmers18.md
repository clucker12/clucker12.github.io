---
title: "[프로그래머스] 순위 (플로이드 워셜)"
date: 2025-12-03
categories: [Algorithm, Programmers]
tags: [플로이드워셜, 그래프, 순위, Java]
layout: single
author_profile: true
read_time: true
share: true
show_tags: true
show_categories: true
toc: true
sidebar:
  nav: "c"
---

# 🏆 Programmers - 순위 (Floyd--Warshall)

## 📌 문제 개요

선수들의 일부 경기 결과만 주어진 상태에서, **모든 상대와의 승패 관계가
완전히 확정되는 선수의 수**를 구하는 문제입니다.\
그래프의 **전이적 승패 관계 전달**이 핵심이며, Floyd--Warshall
알고리즘을 사용해 해결할 수 있습니다.

---

# 🔍 전체 코드

```java
package Programmers;

public class programmers18 {
    public int solution(int n, int[][] results) {

        // wins[a][b] = true  →  a 선수가 b 선수를 이겼다는 뜻
        boolean[][] wins = new boolean[n+1][n+1];

        // 1. 직접 주어진 경기 결과 입력
        for (int[] r : results){
            wins[r[0]][r[1]] = true; // r[0] → r[1]
        }

        // 2. Floyd-Warshall 알고리즘
        //    중간 선수 k를 거쳐가면서
        //    i → k, k → j 라면 i → j 도 true 로 만든다.
        //    즉 모든 승패 관계를 전파함
        for (int k = 1; k <= n; k++) {        // k = 중간 선수
            for (int i = 1; i <= n; i++) {    // i = 출발 선수
                for (int j = 1; j <= n; j++) { // j = 도착 선수

                    // i가 k를 이기고 && k가 j를 이기면
                    // i는 j를 확실히 이긴 것이므로 표시
                    if(wins[i][k] && wins[k][j]){
                        wins[i][j] = true;
                    }
                }
            }
        }

        int answer = 0; // 순위를 확정할 수 있는 선수 수

        // 3. 각 선수 i 에 대해 순위가 확정되는지 확인
        for (int i = 1; i <= n; i++) {
            int cnt = 0;

            // i번 선수와 j번 선수의 승패가 확실한가?
            // wins[i][j] → i가 j를 이김
            // wins[j][i] → i가 j에게 짐
            // 둘 중 하나라도 true면 승패가 확정된 것
            for (int j = 1; j <= n; j++) {
                if(i == j) continue; // 자기 자신은 제외

                if(wins[i][j] || wins[j][i]){
                    cnt++; // j와의 승패 관계가 확실히 정해짐
                }
            }

            // i 선수는 총 n-1명과의 승부가 모두 확정돼야
            // 순위를 정확히 알 수 있음
            if(cnt == n-1){
                answer++;
            }
        }

        return answer;
    }
}

```

---

# 📚 핵심 자료구조 & 알고리즘 설명

## ✅ 1. `boolean[][] wins`

- wins\[a\]\[b\] = true → **a 선수가 b 선수에게 승리**
- 방향 그래프 형태\
- 전이적 관계 확인에 최적화

## ✨ 2. Floyd--Warshall 알고리즘 사용

모든 노드 쌍 (i → j)에 대해,\
중간 노드 k를 이용해 경로가 존재하면 관계를 업데이트합니다.

    i → k, k → j  →  i → j 성립

이 문제에선 **승패 관계의 전파**에 사용됩니다.

---

# 🧠 시간 복잡도

단계 복잡도

---

결과 입력 O(E)
Floyd--Warshall **O(n³)**
순위 확정 확인 O(n²)

➡️ 전체 시간 복잡도: **O(n³)**

---

# 🧩 문제 해결 핵심 로직 요약

1.  **초기 경기 결과 wins 배열에 기록**
2.  **플로이드 워셜로 모든 승패 관계 전파**
3.  **각 선수별로 n-1명 모두와 승패가 확정되면 → 순위 확정**
4.  **그 선수 수를 리턴**

---

# 💡 마무리 & 느낀 점

이 문제는 단순한 그래프 탐색 문제처럼 보이지만 실제로는 **전이적 승패
관계**를 모두 확인해야 하므로 Floyd--Warshall 선택이 필수적입니다.

처음엔 DFS로 접근해볼까 고민했는데, 모든 노드 쌍 관계를 전이적으로
계산하려면 O(n³) 알고리즘이 가장 안정적이라는 점을 다시 느꼈습니다.

특히 그래프 문제에서는\

- "**관계의 전파**"\
- "**모든 쌍 간 연결성 계산**"

이 두 키워드가 보이면 **Floyd--Warshall**을 떠올리는 것이 정석이라는 걸
다시 상기했습니다.

---
