---
title: "[Programmers] 타겟 넘버 (DFS/BFS)"
date: 2025-11-11
categories: [Algorithm, DFS, Programmers]
tags: [DFS, 완전탐색, 재귀, 타겟넘버, Java]
layout: single
author_profile: true
read_time: true
share: true
show_tags: true
show_categories: true
toc: true
sidebar:
  nav: "c"
---

# 💻 문제 설명

주어진 숫자 배열 `numbers`의 각 원소에 `+` 또는 `-`를 붙여서 **target** 숫자를 만드는 경우의 수를 구하는 문제입니다.
문제 링크: [https://school.programmers.co.kr/learn/courses/30/lessons/43165](https://school.programmers.co.kr/learn/courses/30/lessons/43165)

---

## 📜 원본 코드

```java
package Programmers;

public class programmers10 {
    // 전체 경우의 수 중 타겟 넘버를 만드는 경우의 개수를 저장할 변수
    int answer = 0;

    // 메인 메서드: numbers 배열과 target 값을 받아서 경우의 수를 계산
    public int solution(int[] numbers, int target) {
        // DFS 탐색 시작: 초기 깊이(depth)=0, 초기 합(sum)=0
        dfs(0, target, numbers, 0);
        return answer; // 최종적으로 target을 만든 횟수를 반환
    }

    // DFS(깊이 우선 탐색) 재귀 함수
    // depth: 현재 탐색 중인 인덱스
    // target: 목표 숫자
    // numbers: 주어진 숫자 배열
    // sum: 현재까지의 계산 결과 합
    public void dfs(int depth, int target, int[] numbers, int sum) {
        // 모든 숫자를 다 사용한 경우 (리프 노드에 도달)
        if (depth == numbers.length) {
            // 누적된 합(sum)이 target과 같으면 경우의 수 증가
            if (sum == target) {
                answer++;
            }
            return; // 더 이상 탐색할 필요 없으므로 종료
        }

        // 현재 숫자를 더하는 경우
        dfs(depth + 1, target, numbers, sum + numbers[depth]);
        // 현재 숫자를 빼는 경우
        dfs(depth + 1, target, numbers, sum - numbers[depth]);
    }
}
```

---

## ⚙️ 접근 방법

1. **모든 경우의 수 탐색 (완전 탐색)**

   - 각 숫자에 대해 `+` 혹은 `-`를 붙이는 두 가지 선택지를 가진다.
   - 따라서 가능한 조합의 수는 `2^n` (n은 numbers의 길이).

2. **DFS (깊이 우선 탐색)로 완전 탐색 구현**

   - 깊이(`depth`)가 배열의 길이에 도달할 때까지 재귀적으로 호출한다.
   - 리프 노드에서 현재까지의 합(`sum`)이 target과 같으면 경우의 수를 증가시킨다.

3. **시간 복잡도**
   - 각 숫자마다 두 가지 선택지를 탐색하므로 **O(2^N)**.
   - `numbers.length <= 20`이므로 충분히 완전 탐색이 가능.

---

## 🔍 주요 메서드 및 핵심 로직

### 🔹 `dfs(int depth, int target, int[] numbers, int sum)`

- 현재 인덱스(`depth`)에서 다음 숫자를 **더하거나 빼는** 두 가지 경우를 재귀적으로 탐색.
- 깊이가 배열의 길이에 도달하면 `sum == target`인지 검사.

📌 **핵심 포인트**

- “모든 가능한 경우의 수”를 전부 탐색하되, 불필요한 가지치기가 필요 없는 전형적인 완전 탐색 문제.

---

## 🧠 사용한 자료구조 및 알고리즘

- **자료구조:** 단순 배열 (int[])
- **알고리즘:** DFS (Depth First Search)
- **탐색 구조:** 완전 이진 트리 형태로 전 탐색 수행

---

## ⏱️ 시간 복잡도 분석

- DFS를 통해 각 단계에서 두 가지 선택 → 깊이 `n`이면 `2^n`개의 탐색 수행
- **시간 복잡도:** O(2^n)
- **공간 복잡도:** O(n) (재귀 호출 스택)

---

## 💡 마무리 및 느낀 점 (2025-11-11)

이 문제는 DFS의 기본 원리를 가장 직관적으로 보여주는 문제다.  
모든 경우의 수를 탐색하면서 재귀 호출의 구조와 종료 조건(기저 조건)의 중요성을 다시금 체감했다.  
특히, "리프 노드에서의 판단"이라는 DFS의 전형적인 패턴이 잘 드러나며,  
이 문제를 통해 DFS의 기초를 탄탄히 다질 수 있었다.

---
