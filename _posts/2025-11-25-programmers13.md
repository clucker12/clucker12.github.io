---
title: "[프로그래머스] 단어 변환 (DFS)"
date: 2025-11-25
categories: [Algorithm, Programmers]
tags: [DFS, 그래프, 단어변환, Java]
layout: single
author_profile: true
read_time: true
share: true
show_tags: true
show_categories: true
toc: true
sidebar:
  nav: "c"
---

# 🔤 단어 변환 (DFS 풀이)

프로그래머스의 **단어 변환** 문제는 시작 단어에서 목표 단어까지 도달하기 위한  
**최소 변환 횟수**를 구하는 전형적인 그래프 탐색 문제이다.

한 번에 **한 글자만 다른 단어로만 이동 가능**하며,  
주어진 `words` 목록에 포함된 단어만 사용할 수 있다.

DFS + 백트래킹을 이용해 모든 가능한 경로를 탐색하며  
`min` 값을 갱신하는 방식으로 풀이했다.

---

## 📌 문제 코드 (Java)

```java
package Programmers;

public class programmers13 {
    // 최단 횟수를 저장하는 변수 (기본값은 Integer.MAX_VALUE)
    static int min = Integer.MAX_VALUE;

    // solution 메서드 - 시작 단어(begin)와 목표 단어(target), 가능한 단어 리스트(words)를 받아
    // 시작 단어에서 목표 단어로 변환하는 최소 횟수를 구하는 함수
    public int solution(String begin, String target, String[] words) {
        int n = words.length;  // words 배열의 길이 (단어의 개수)
        boolean[] visited = new boolean[n];  // 방문한 단어를 체크하는 배열

        // dfs 호출: 시작 단어(begin), 목표 단어(target), 단어 리스트(words), 방문 배열(visited), 변환 횟수(cnt) 전달
        dfs(begin, target, words, visited, 0);

        // 만약 min이 Integer.MAX_VALUE로 남아있다면, 변환이 불가능하다는 의미이므로 0을 반환
        // 그렇지 않으면 최소 변환 횟수(min)를 반환
        return min == Integer.MAX_VALUE ? 0 : min;
    }

    // dfs 메서드 - 깊이 우선 탐색을 이용하여 단어 변환의 최소 횟수를 찾는 함수
    public static void dfs(String begin, String target, String[] words, boolean[] visited, int cnt) {
        // 현재 begin 단어가 target과 같다면, 변환이 완료된 것이므로 최소 횟수 갱신
        if (begin.equals(target)) {
            min = Math.min(min, cnt);  // min과 현재 cnt 값 중 더 작은 값을 min에 저장
            return;  // 종료
        }

        // words 배열을 순차적으로 탐색
        for (int k = 0; k < words.length; k++) {
            // 이미 방문한 단어는 건너뛰기
            if (visited[k]) continue;

            int count = 0;  // begin과 words[k]의 일치하는 문자 개수를 셈
            int n = words[k].length();  // 단어의 길이 (begin과 words[k]는 길이가 같음)

            // begin과 words[k]를 비교하여 일치하는 문자의 개수를 셈
            for (int i = 0; i < n; i++) {
                if (words[k].charAt(i) == begin.charAt(i)) {
                    count++;
                }
            }

            // 만약 두 단어가 딱 하나만 다르면, 즉 일치하는 문자 개수가 begin.length() - 1일 경우
            // 해당 단어를 선택하고 dfs를 진행
            if (count == begin.length() - 1) {
                visited[k] = true;  // 해당 단어는 방문했다고 표시
                dfs(words[k], target, words, visited, cnt + 1);  // words[k]로 변환한 뒤 재귀 호출
                visited[k] = false;  // dfs 호출 후 다시 방문 처리 해제 (백트래킹)
            }
        }
    }
}

```

---

## 🧠 핵심 아이디어

1. 단어는 그래프의 노드

각 단어는 ‘정점’이고,
두 단어가 한 글자만 다르면 간선이 존재하는 구조.

2. DFS로 모든 경로 탐색

- 한 글자 차이 단어만 이동
- 도달할 수 있는 모든 경로를 탐색
- 도중에 target을 만나면 min 갱신

3. 백트래킹

경로 탐색 후 visited를 다시 해제해
다른 경로도 탐색 가능하게 함.

---

## 🔍 주요 메서드 & 로직 설명

solution()

- visited 배열 초기화
- DFS 탐색 시작
- 변환 불가능하면 0 반환

dfs()

- begin == target → 최소값 업데이트
- 단어 리스트 순회
- 한 글자 차이인지 비교
- 조건 만족 시 다음 단어로 DFS
- 탐색 후 방문 해제(백트래킹)

---

## 🧬 사용된 자료구조

| 자료구조            | 역할                          |
| ------------------- | ----------------------------- |
| `boolean[] visited` | 이미 방문한 단어 중복 방지    |
| `String[] words`    | 변환 가능한 단어 목록         |
| DFS(재귀)           | 모든 경로 탐색 후 최소값 갱신 |

---

## ⏱ 시간 복잡도

최악의 경우

- 모든 단어를 대상으로 DFS 완전 탐색
- 각 깊이에서 최대 n개의 단어 비교 → O(n)
- 한 단어 비교 때 글자 수 L만큼 비교 → O(L)
  ➡ O(n² × L)
  (단어 길이 L이 최대 10 이하라 사실상 O(n²))

---

## 💡 느낀점 / 문제 접근

이 문제는 "그래프 문제"라고 인식하자마자 쉽게 풀 수 있었다.
단어 자체를 노드,
한 글자 차이를 간선으로 바라보는 관점이 핵심이었다.

초기에 BFS가 더 효율적일지 고민했지만,
DFS + 백트래킹으로도 충분히 최소값을 구할 수 있었다.
특히 “한 글자만 다른지 판별하는 로직”이 문제의 핵심 부분이며,
이 조건을 정확하게 구성하는 것이 문제의 반 이상이라고 느꼈다.
