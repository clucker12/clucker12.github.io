---
title: "[백준 17406번] 배열 돌리기 4 🔄"
date: 2025-11-04
categories: [Baekjoon, Implementation, DFS, BruteForce]
tags: [백준, 완전탐색, 구현, DFS, 순열, 배열돌리기]
layout: single
author_profile: true
read_time: true
share: true
show_tags: true
show_categories: true
toc: true
sidebar:
  nav: "c"
---

## 📘 문제 개요

> **문제 번호:** 17406  
> **문제 이름:** 배열 돌리기 4  
> **난이도:** 골드 4  
> **문제 링크:** [https://www.acmicpc.net/problem/17406](https://www.acmicpc.net/problem/17406)

---

## 🧩 문제 설명

`N × M` 배열이 주어지고,  
`(r, c, s)` 형태의 **회전 연산**이 여러 개 주어진다.

각 연산은 `(r, c)`를 중심으로 한 정사각형 범위를 시계 방향으로 한 칸 회전시키며,  
**회전 순서를 바꿔가며** 최종 배열의 “행의 합 중 최솟값”이 가장 작아지는 경우를 찾아야 한다.

즉,

> 모든 연산 순서를 고려해 최적의 순서를 찾는 **완전탐색(Brute Force)** 문제이다.

---

## 💡 접근 방법

1. **K개의 회전 연산 순서를 모두 고려 (순열 생성)**
   - `K ≤ 6` 이므로 `6! = 720` → 전부 탐색 가능.
   - DFS로 가능한 모든 순서를 생성한다.
2. **각 순열에 대해 배열 복사 후 순서대로 회전 수행**
3. **회전이 끝난 배열의 각 행 합 계산**
   - 행 합의 최솟값을 구해 전체 최소값을 갱신한다.
4. **최종 최소값 출력**

---

## 💻 전체 코드

```java
package Baekjoon;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.*;

public class baekjoon17406 {
    // 전역 변수 선언
    static int N, M, K;            // 배열 크기 N×M, 회전 연산 개수 K
    static int[][] board;          // 원본 배열
    static int[][] rotation;       // 회전 연산 정보 (r, c, s)
    static boolean[] visited;      // 순열 생성 시 사용 여부 체크
    static int[] order;            // 회전 연산 순서 저장용
    static int answer = Integer.MAX_VALUE; // 결과(최소값)

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        // --- 입력 ---
        st = new StringTokenizer(br.readLine());
        N = Integer.parseInt(st.nextToken());
        M = Integer.parseInt(st.nextToken());
        K = Integer.parseInt(st.nextToken());

        board = new int[N][M];
        rotation = new int[K][3]; // 회전 연산 K개, 각 (r, c, s)

        // 배열 입력 받기
        for (int i = 0; i < N; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < M; j++) {
                board[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        // 회전 연산 입력 받기
        for (int i = 0; i < K; i++) {
            st = new StringTokenizer(br.readLine());
            rotation[i][0] = Integer.parseInt(st.nextToken()); // r
            rotation[i][1] = Integer.parseInt(st.nextToken()); // c
            rotation[i][2] = Integer.parseInt(st.nextToken()); // s
        }

        visited = new boolean[K]; // 순열 생성용 방문 배열
        order = new int[K];       // 순열 결과 저장

        // 모든 회전 순서 탐색 시작
        dfs(0);

        // 결과 출력
        System.out.println(answer);
    }

    // ----------------------------------------------------
    // 순열을 이용해 모든 회전 순서 조합 생성
    // ----------------------------------------------------
    static void dfs(int depth) {
        if (depth == K) { // K개 모두 선택했을 때
            // 원본 배열 복사 (매 순열마다 원래 상태에서 시작)
            int[][] temp = new int[N][M];
            for (int i = 0; i < N; i++)
                temp[i] = board[i].clone();

            // 순열 순서대로 회전 연산 수행
            for (int idx : order) {
                rotate(temp, rotation[idx]);
            }

            // 회전이 끝난 배열의 "값" 계산 후 최소값 갱신
            answer = Math.min(answer, calc(temp));
            return;
        }

        // 아직 선택 안 된 회전 연산을 하나씩 선택
        for (int i = 0; i < K; i++) {
            if (!visited[i]) {
                visited[i] = true;
                order[depth] = i;  // 현재 위치에 i번째 연산 선택
                dfs(depth + 1);    // 다음 깊이로 이동
                visited[i] = false; // 백트래킹 (선택 취소)
            }
        }
    }

    // ----------------------------------------------------
    // 회전 연산 수행 함수
    // arr : 현재 배열
    // rot : [r, c, s]
    // ----------------------------------------------------
    static void rotate(int[][] arr, int[] rot) {
        int r = rot[0] - 1; // 입력은 1-based → 배열은 0-based
        int c = rot[1] - 1;
        int s = rot[2];

        // 바깥 layer부터 안쪽 layer까지 순서대로 회전
        for (int layer = 1; layer <= s; layer++) {
            int top = r - layer;
            int left = c - layer;
            int bottom = r + layer;
            int right = c + layer;

            // 좌상단 값 임시 저장 (덮어쓰기 방지)
            int temp = arr[top][left];

            // 왼쪽 세로 ↓ 이동
            for (int i = top; i < bottom; i++) {
                arr[i][left] = arr[i + 1][left];
            }

            // 아래쪽 가로 ← 이동
            for (int i = left; i < right; i++) {
                arr[bottom][i] = arr[bottom][i + 1];
            }

            // 오른쪽 세로 ↑ 이동
            for (int i = bottom; i > top; i--) {
                arr[i][right] = arr[i - 1][right];
            }

            // 위쪽 가로 → 이동
            for (int i = right; i > left + 1; i--) {
                arr[top][i] = arr[top][i - 1];
            }

            // 저장해 둔 값 삽입
            arr[top][left + 1] = temp;
        }
    }

    // ----------------------------------------------------
    // 배열의 "값" 계산: 각 행의 합 중 최솟값
    // ----------------------------------------------------
    static int calc(int[][] arr) {
        int min = Integer.MAX_VALUE;
        for (int i = 0; i < N; i++) {
            int sum = 0;
            for (int j = 0; j < M; j++) {
                sum += arr[i][j];
            }
            min = Math.min(min, sum);
        }
        return min;
    }
}

```

---

## 🔍 핵심 로직 요약

| 구분                | 주요 내용                               | 설명                                   |
| ------------------- | --------------------------------------- | -------------------------------------- |
| 🧠 순열 생성        | `dfs()` 재귀로 모든 회전 순서 조합 생성 | 회전 순서에 따라 결과가 달라짐         |
| 🔁 배열 회전        | `rotate()`                              | layer 단위로 테두리만 회전 (시계 방향) |
| 📉 배열 최소값 계산 | `calc()`                                | 모든 행 합 중 최솟값 반환              |
| 🧩 백트래킹         | `visited[]` 관리                        | 순열에서 중복 방지 및 복원 처리        |

---

## ⚙️ 시간 복잡도

| 단계      | 복잡도            | 설명                                    |
| --------- | ----------------- | --------------------------------------- |
| 순열 생성 | O(K!)             | 최대 6! = 720                           |
| 회전 수행 | O(K × N × M)      | 각 순열마다 배열 회전                   |
| 전체      | **O(K! × N × M)** | 완전탐색이지만 N, M, K 작아 충분히 가능 |

---

## 🧱 사용된 자료구조

| 자료구조            | 역할                            |
| ------------------- | ------------------------------- |
| `int[][] board`     | 원본 배열 저장                  |
| `int[][] rotation`  | 회전 연산 정보 `(r, c, s)` 저장 |
| `boolean[] visited` | 순열 생성 시 방문 관리          |
| `int[] order`       | 회전 순서 저장                  |
| `int[][] temp`      | 각 순열마다 복사본 배열         |

---

## ✨ 주요 메서드 설명

dfs(int depth)

- 회전 순서를 만들기 위한 순열 생성용 DFS.
- 모든 연산 순서를 탐색해 최솟값을 계산한다.

rotate(int[][] arr, int[] rot)

- 주어진 (r, c, s) 중심으로 사각형의 테두리를 한 칸씩 시계 방향으로 회전.
- layer 단위 회전 구조가 핵심.

calc(int[][] arr)

- 회전이 완료된 배열에서 각 행의 합을 구해 그중 최소값을 반환.

---

## 💭 느낀점

이 문제는 단순한 회전 구현 문제가 아니라
**“모든 순서의 조합을 고려해야 하는 완전탐색 + 구현 문제”**였다.

초기에 회전 순서만 바꿔도 결과가 달라진다는 점을 이해하는 데 시간이 걸렸다.
이후 DFS로 순열을 만들고, 각 순열마다 배열을 복사하여 독립적으로 회전시켜야 한다는 아이디어가 핵심이었다.

또한, rotate() 메서드 구현에서 layer(테두리) 단위로 이동하는 구조는
처음엔 복잡했지만 실제로는 “배열의 가장자리를 따라 한 칸씩 이동”이라는 단순한 규칙으로 해결 가능했다.

👉 이 문제를 통해 다음을 배웠다:

- 완전탐색(Brute Force)과 구현(Implementation)의 조합은 디테일이 중요하다.

- “회전”이나 “이동” 문제에서는 경계값(layer, index) 관리가 핵심이다.

- 백트래킹과 배열 복사를 함께 다루는 연습에 매우 유용한 문제였다.
