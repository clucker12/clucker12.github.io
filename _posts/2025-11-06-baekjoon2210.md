---
title: "[백준 2210번] 숫자판 점프"
date: 2025-11-06
categories: [Baekjoon, DFS]
tags: [백준, DFS, 완전탐색, 해시셋, 브루트포스]
layout: single
author_profile: true
read_time: true
share: true
show_tags: true
show_categories: true
toc: true
sidebar:
  nav: "c"
---

# 🧩 백준 2210번: 숫자판 점프

## 📘 문제 설명

5×5 숫자판에서 임의의 칸에서 시작하여 인접한 상하좌우 칸으로 다섯 번 이동하면서 만들어지는 **6자리 수**가 있습니다.  
이때, 가능한 서로 다른 6자리 수의 개수를 구하는 문제입니다.

---

## 🧠 접근 방법

이 문제는 **DFS(깊이 우선 탐색)** 과 **백트래킹**을 사용하는 대표적인 완전 탐색 문제입니다.

- 시작 위치는 25칸(5×5) 중 어디든 가능.
- 각 칸에서 4방향(상, 하, 좌, 우)으로 이동 가능.
- 이동은 총 5번이므로 깊이는 6자리 문자열이 될 때까지 진행.
- 중복된 숫자 조합은 `HashSet`으로 관리.

결국 **모든 경로를 탐색하면서 가능한 모든 6자리 수를 만들어**,  
중복을 제거한 후 **집합의 크기**를 출력하면 됩니다.

---

## 💻 전체 코드

```java
package Baekjoon;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.*;

// 백준 2210번: 숫자판 점프
public class baekjoon2210 {

    // 5x5 숫자판
    static int[][] board = new int[5][5];

    // 중복을 피하기 위해 HashSet 사용 (만들어진 6자리 숫자를 저장)
    static Set<String> set = new HashSet<>();

    // 상하좌우 방향 이동을 위한 좌표 변화량
    static int[] dx = {-1, 1, 0, 0};
    static int[] dy = {0, 0, -1, 1};

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        // 숫자판 입력 받기
        for (int i = 0; i < 5; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < 5; j++) {
                board[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        // 모든 칸에서 DFS 탐색 시작
        for (int i = 0; i < 5; i++) {
            for (int j = 0; j < 5; j++) {
                // 시작 위치 (i, j)에서 깊이 0, 초기 문자열은 현재 칸의 숫자
                dfs(i, j, 0, "" + board[i][j]);
            }
        }

        // HashSet에 저장된 서로 다른 6자리 수의 개수를 출력
        System.out.println(set.size());
    }

    /**
     * 깊이 우선 탐색 (DFS)
     * @param x 현재 행 좌표
     * @param y 현재 열 좌표
     * @param depth 현재 이동 횟수 (0부터 시작)
     * @param num 현재까지 만든 숫자 문자열
     */
    static void dfs(int x, int y, int depth, String num) {
        // depth == 5이면 현재까지 6자리 숫자 완성
        if (depth == 5) {
            set.add(num);  // HashSet에 추가 (자동으로 중복 제거)
            return;
        }

        // 상, 하, 좌, 우 네 방향으로 이동 시도
        for (int dir = 0; dir < 4; dir++) {
            int nx = x + dx[dir];  // 다음 행 좌표
            int ny = y + dy[dir];  // 다음 열 좌표

            // 보드 범위를 벗어나지 않으면 이동
            if (nx >= 0 && nx < 5 && ny >= 0 && ny < 5) {
                // 다음 칸으로 이동하면서 숫자 이어붙이기
                dfs(nx, ny, depth + 1, num + board[nx][ny]);
            }
        }
    }
}

```

---

## ⚙️ 주요 메서드 및 핵심 로직

| 메서드                                     | 설명                                                           |
| ------------------------------------------ | -------------------------------------------------------------- |
| `dfs(int x, int y, int depth, String num)` | 현재 좌표에서 DFS 탐색 수행, 6자리 숫자 완성 시 HashSet에 저장 |
| `HashSet<String> set`                      | 중복 방지용 자료구조 (동일한 숫자는 한 번만 저장됨)            |
| `dx[], dy[]`                               | 상하좌우 이동을 위한 방향 벡터                                 |

- DFS의 깊이는 최대 6 (이동 5번 + 시작칸 1개)
- 각 칸에서 4방향으로 분기되므로 **최대 탐색 경우의 수**는 약 `25 × 4⁵` ≈ 25,600 정도로 충분히 탐색 가능

---

## ⏱ 시간 복잡도

- 각 칸에서 최대 4방향으로 깊이 5까지 탐색  
  → **O(25 × 4⁵) = O(25,600)**
- 중복 검사는 `HashSet`의 평균 시간복잡도 O(1)이므로 전체적으로 효율적임.

---

## 💡 마무리 및 느낀점

처음엔 단순 브루트포스라 생각했지만,  
DFS 깊이를 5까지만 제한하니 경우의 수가 충분히 작아 효율적으로 동작했습니다.

이 문제의 핵심은 **중복 제거와 경계 조건 처리**이며,  
특히 HashSet을 이용한 중복 제거가 핵심 포인트입니다.

> 완전 탐색 + 백트래킹의 전형적인 구조로,  
> DFS의 기본기를 복습하기에 좋은 문제였습니다. 🌱
