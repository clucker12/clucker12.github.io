---
title: "[백준 17136번] 색종이 붙이기 🩵"
date: 2025-11-05
categories: [Baekjoon, DFS, 백트래킹]
tags: [백준, 완전탐색, DFS, 백트래킹, 브루트포스]
layout: single
author_profile: true
read_time: true
share: true
show_tags: true
show_categories: true
toc: true
sidebar:
  nav: "c"
---

## 🧩 문제 개요

10×10 크기의 종이에 1(붙여야 하는 칸)과 0(빈 칸)이 주어질 때,  
1×1부터 5×5까지 크기의 색종이를 각각 **5장씩만 사용 가능**하다.  
모든 1을 덮되, **가장 적은 색종이 수를 사용**해야 한다.

덮을 수 없는 경우는 `-1`을 출력한다.

---

## 💡 접근 방법

1. 완전탐색(Brute Force) + 백트래킹을 이용한다.
2. 10×10 보드의 각 칸을 순차적으로 확인하며, `1`인 경우 **색종이를 붙일 수 있는지 검사**한다.
3. **5×5 → 1×1 순서로 시도**하여 큰 색종이부터 덮는다.  
   (큰 색종이를 먼저 사용하면 탐색 깊이가 줄어 효율적)
4. 덮을 수 있으면:
   - 색종이 붙임 → 남은 개수 감소 → 다음 칸으로 DFS 진행
   - 되돌릴 때 원상복구 (백트래킹)
5. 모든 칸이 덮였을 때 최소값(`answer`)을 갱신한다.

---

## 💻 전체 코드

```java
package Baekjoon;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class baekjoon17136 {
    // 10x10 크기의 종이 (0: 빈칸, 1: 색종이 붙여야 할 칸)
    static int[][] board = new int[10][10];

    // 색종이 남은 개수 (1x1 ~ 5x5까지 각각 5장씩)
    // 인덱스를 1~5로 쓰기 위해 0번째는 더미 값
    static int[] remain = {0, 5, 5, 5, 5, 5};

    // 최소 사용 색종이 수 (답)
    static int answer = Integer.MAX_VALUE;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        // 입력: 10줄에 걸쳐 10개의 숫자 (0 또는 1)
        for (int i = 0; i < 10; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < 10; j++) {
                board[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        // DFS 탐색 시작 (pos = 0: 첫 번째 칸, used = 0: 사용한 색종이 수)
        dfs(0, 0);

        // 모든 경우를 탐색했는데도 덮을 수 없는 경우 => -1 출력
        System.out.println(answer == Integer.MAX_VALUE ? -1 : answer);
    }

    /**
     * DFS (백트래킹)
     * pos  : 현재 검사 중인 칸 번호 (0~99)
     * used : 지금까지 사용한 색종이 개수
     */
    static void dfs(int pos, int used) {
        // 이미 찾은 최소값보다 많이 쓴 경우는 더 볼 필요 없음 (가지치기)
        if (used >= answer) return;

        // 아직 100칸 다 안봤으면, 다음 1을 찾을 때까지 pos 이동
        while (pos < 100) {
            int x = pos / 10;  // 행
            int y = pos % 10;  // 열
            if (board[x][y] == 1) break;  // 색종이 붙여야 하는 칸 찾으면 중단
            pos++;  // 다음 칸으로 이동
        }

        // 100칸(0~99) 다 돌았으면 모든 칸이 덮인 상태 → 답 갱신
        if (pos == 100) {
            answer = Math.min(answer, used);
            return;
        }

        // 현재 칸 좌표 계산
        int x = pos / 10;
        int y = pos % 10;

        // 혹시 현재 칸이 0이면 (이미 덮인 경우) 다음 칸으로
        if (board[x][y] == 0) {
            dfs(pos + 1, used);
            return;
        }

        // 색종이 크기를 5x5 → 1x1 순으로 시도
        for (int size = 5; size >= 1; size--) {
            // 해당 크기의 색종이 다 썼으면 건너뜀
            if (remain[size] == 0) continue;

            // 현재 위치에 size 크기의 색종이를 붙일 수 있으면
            if (canAttach(x, y, size)) {
                // 붙이기 (board값을 0으로 바꿈)
                attach(x, y, size, 0);
                remain[size]--;  // 남은 색종이 개수 줄임

                // 다음 칸 탐색
                dfs(pos + 1, used + 1);

                // 되돌리기 (원상복구, 백트래킹)
                attach(x, y, size, 1);
                remain[size]++;
            }
        }
    }

    /**
     * 현재 (x, y)에 size 크기의 색종이를 붙일 수 있는지 확인
     */
    static boolean canAttach(int x, int y, int size) {
        // 색종이가 종이 범위를 벗어나면 불가능
        if (x + size > 10 || y + size > 10) return false;

        // size x size 영역 안에 1이 아닌 칸이 있으면 불가능
        for (int i = x; i < x + size; i++) {
            for (int j = y; j < y + size; j++) {
                if (board[i][j] == 0) return false;
            }
        }
        return true; // 모두 1이면 붙일 수 있음
    }

    /**
     * 색종이 붙이기 / 떼기
     * val = 0 → 색종이 붙이기 (1 → 0)
     * val = 1 → 색종이 떼기 (0 → 1)
     */
    static void attach(int x, int y, int size, int val) {
        for (int i = x; i < x + size; i++) {
            for (int j = y; j < y + size; j++) {
                board[i][j] = val;
            }
        }
    }
}

```

---

## 🧠 핵심 로직 & 주요 메서드

### ✅ `dfs(int pos, int used)`

- **목적**: 현재까지 사용한 색종이 수(`used`)와 현재 위치(`pos`)를 기반으로 재귀 탐색
- **가지치기**: 이미 최소값(`answer`)보다 많이 사용한 경우 탐색 중단
- **백트래킹**: 색종이 붙였다가 다시 떼는 구조로 모든 조합을 탐색

```java
if (used >= answer) return; // 가지치기
while (pos < 100 && board[pos/10][pos%10] == 0) pos++; // 다음 1 탐색
if (pos == 100) { // 모든 칸 덮음
    answer = Math.min(answer, used);
    return;
}
```

---

### ✅ `canAttach(int x, int y, int size)`

- 해당 좌표에 **size×size 크기의 색종이를 붙일 수 있는지 검사**
- 경계 밖으로 나가거나, 0이 있는 경우 불가능

```java
for (int i = x; i < x + size; i++) {
    for (int j = y; j < y + size; j++) {
        if (board[i][j] == 0) return false;
    }
}
return true;
```

---

### ✅ `attach(int x, int y, int size, int val)`

- 색종이를 **붙이거나 떼는 함수**
- `val=0`이면 색종이 붙임, `val=1`이면 되돌림

```java
for (int i = x; i < x + size; i++) {
    for (int j = y; j < y + size; j++) {
        board[i][j] = val;
    }
}
```

---

## 📊 시간 복잡도

- 최악의 경우 **O(4^(100))**에 가까운 완전탐색이지만,  
  백트래킹과 가지치기를 통해 실제 탐색량은 크게 줄어든다.
- 색종이 개수가 제한(각 5개)이므로, 탐색 깊이는 25 이내로 제한된다.

➡️ **실제 수행 시간은 충분히 통과 가능 (백트래킹 필수)**

---

## 🧩 사용된 자료구조

| 자료구조        | 역할                                  |
| --------------- | ------------------------------------- |
| `int[][] board` | 종이의 상태 (1: 덮을 칸, 0: 빈칸)     |
| `int[] remain`  | 각 색종이 크기별 남은 개수 (1~5 크기) |
| `int`           | 최소 색종이 수 저장용                 |

---

## 💬 느낀점

이 문제는 단순한 완전탐색이 아니라, **효율적인 백트래킹 설계**가 핵심이었다.  
큰 색종이부터 붙이는 전략과 가지치기를 통해 시간 초과를 방지할 수 있었다.  
단순히 DFS만 구현하는 것이 아니라,  
“**탐색 순서와 가지치기 타이밍을 어떻게 설정할 것인가**”의 중요성을 느낀 문제였다.
